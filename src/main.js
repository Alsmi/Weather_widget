var ReactDOM = require('react-dom');
var React = require('react');
var WidgetApp = require('./components/WidgetApp.jsx');

ReactDOM.render(
    <WidgetApp />,
    document.getElementById('content')
);